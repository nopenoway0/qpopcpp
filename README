compile:
mkdir build
cd build
cmake ..
Then either open the produced solution file in Visual Studio or run msbuild QPOP.sln.
The executable will be produced in the executable folder in the root directory. Copy the files lol.txt and play.bmp into the same directory as the executable
the following libraries are used: websocketpp, all dependencies of boost.Asio, and wxWidgets.

Usage:
Start up the program and start League of Legends.
Start up the companion app on your android or iphone
Enter your IP (will add IP display in future) and Auth code as shown on the desktop app
Enter a match queue