Compilation:
Set the correct include directories - required includes require qpop sepcific headers and the standard boost includes, and websocket includes as well.

The following libraries will need to be linked: libboost_thread